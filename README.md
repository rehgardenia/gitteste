# meus passos 
- checar versao :
```git version```
- limpar a tela:
```clear ```
- abrir diretorio com o git bash  e dentro dele iniciar o repositorio :
```git init``` 
- (primeiro acesso )configurar dados com:
```git config --local user.name "nome"```
```git config --local user.email "email@dkff.com"```
- (abre o editor e abre o diretorio e cria um arquivo html)
- (terminal) adicionar o arquivo para ser monitorado
```git add / git add  arquivo.html```
-  mostra se tem agum arquivo pedente
```git status```
- salva o arquivo mando para o repositorio local
```git commit -m "titulo"``` 
(se vc adicionar algo no arquivo vai mostrar que tem novo conteudo, entao se tera que add/commit novamente -criando linha de tempo)
- mostra todos os commits
```git --log ``` (da para formarta)
- cria um repo no git hub e conecta o git ao github
```
 git remote add origin https:// link do github.git

 git brash -m  main
 
 git push -u origin main (empura o projeto local para o github)
 ```
// video o moco add um reamed.md
 ele add o arquivo dps commit
 e dps git push
